# 1. Предпосылки создания компьютера

1. Эдисоном было открыто явление термоэлектронной эмиссии, что стало толчком к созданию диода и триода.
2. Еще одним открытием, стали правила логики, описанный Булем.
3. Создание электронного реле Бонч-Бруевичем, на базе этого реле был изобретен триггер

# 2. История создания компьютера

1. В ЭВМ первого поколения использовались электронные лампы.
2. В ЭВМ второго поколения использовались транзисторы.
3. В ЭВМ третьего поколения использовались транзисторы, диоды, конденсаторы, резисторы. 
4. В ЭВМ четвертого поколения использовались микропроцессоры.

# 3. Представление чисел в позиционных системах счисления

Число записывается в виде суммы, в которой каждое слагаемое - это значение цифры, умноженной на вес разряда.
<img src="/images/tsis/positional-system-formula.png" />

# 4. Перевод чисел из одной системы счисления в другую (целая часть).

Делим исходное число на основание искомого числа и записываем остаток до тех пор, пока неполное частное не будет 
равно нулю. Полученные остатки записываем в обратном порядке.

| Деление | Целое частное | Остаток |
|---------|---------------|---------|
| 10 / 2  | 5             | 0       |
| 5 / 2   | 2             | 1       |
| 2 / 2   | 1             | 0       |
| 1 / 2   | 0             | 1       |

# 5. Перевод чисел из одной системы счисления в другую (дробная часть).

Умножаем дробную часть на основание искомого числа и записываем полученную в результате умножения целую часть. 
Повторяем операцию с дробной частью полученного числа до тех пор, пока не получится целое число, либо до необходимого 
количества знаков после запятой.

| Умножение | Результат | Целая часть | Остаток |
|-----------|-----------|-------------|---------|
| 0.1 × 2   | 0.2       | 0           | 0.2     |
| 0.2 × 2   | 0.4       | 0           | 0.4     |
| 0.4 × 2   | 0.8       | 0           | 0.8     |
| 0.8 × 2   | 1.6       | 1           | 0.6     |

# 6. Двоичная система счисления. Достоинства и недостатки

Достоинства: в простоте реализации процессов хранения, передачи и обработки информации на компьютере. Для ее реализации 
нужны элементы с двумя возможными состояниями.

Недостаток: длинные числа, трудны для восприятия человеком.

# 7. Шестнадцатеричная система счисления. Достоинства и недостатки

Достоинства: более компактное представление данных и удобство работы с бинарным представлением чисел

Недостаток: трудны для восприятия человеком.

# 8. Единицы измерения информации

1 бит — минимальная единица измерения информации;
1 байт — 8 бит; 
1 килобайт — 1024 байт (KB); 2 в 10-й степени байт
1 мегабайт — 1024 килобайт (MB); 2 в 20-й степени байт
1 гигабайт — 1024 мегабайт (GB); 2 в 30-й степени байт
1 терабайт — 1024 гигабайт (TB); 2 в 40-й степени байт
1 эксабайт — 1024 гигабайт (EB); 2 в 50-й степени байт
1 зеттабайт — 1024 эксабайт (ZB); 2 в 60-й степени байт
1 йоттабайт — 1024 зеттабайт (YB). 2 в 70-й степени байт

# 9. Понятие разряда. Триггер, как модель разряда

Разряд (позиция, место) — это структурный элемент представления чисел в позиционных системах счисления.

Триггер — это электронная схема, применяемая для запоминания одного разряда двоичного кода. Триггер имеет два 
устойчивых состояния, одно из которых соответствует двоичной единице, а другое — двоичному нулю

# 10. Регистр

Регистры – это специальные ячейки памяти, расположенные процессоре. Работа с регистрами выполняется намного быстрее, 
чем с ячейками оперативной памяти.

# 11. Память прямого доступа Организация. Физический адрес. Разрядность ячейки памяти.

Одним из способов обмена данными с ВУ является обмен в режиме прямого доступа к памяти (ПДП). В этом режиме обмен 
данными между ВУ и основной памятью ЭВМ происходит без участия процессора. Обменом в режиме ПДП управляет не программа, 
выполняемая процессором, а электронные схемы, внешние по отношению к процессору. Обычно схемы, управляющие обменом в 
режиме ПДП, размещаются или в специальном контроллере, который называется контроллером прямого доступа к памяти, или в 
контроллере самого ВУ.

# 12. Представление чисел в разрядной сетке машины

<div className="flex justify-center">
    <img src="/images/tsis/numbers-in-bit-grid.png" />
</div>

Слева изображена сетка условной машины в режиме с фиксированной точкой, справа − в режиме с плавающей точкой. Заметим,
что знак числа имеет двоичную природу, так как может принимать только два значения: плюс или минус. В машинах принято 
плюс изображать нулем, а минус − единицей, хотя в принципе можно сделать и наоборот.

# 13. Представление беззнаковых чисел в форме с фиксированной точкой в разрядной сетке

1. число переводится в двоичную систему;
2. результат дополняется нулями слева в пределах выбранного формата;

# 14. Представление знаковых чисел в форме с фиксированной точкой в разрядной сетке

Положительное:
1. число переводится в двоичную систему;
2. результат дополняется нулями слева в пределах выбранного формата;
3. последний разряд слева является знаковым, в положительном числе он равен 0.

Отрицательное:
1. число без знака переводится в двоичную систему;
2. результат дополняется нулями слева в пределах выбранного формата;
3. полученное число переводится в обратный код (нули заменяются единицами, а единицы - нулями);
4. к полученному коду прибавляется 1.

# 15. Представление чисел в форме с плавающей точкой

Вещественное число может быть представлено в экспоненциальном виде, например:

16000000(10) = 0,16⋅10^8

−0,0000156(10)=−0,156⋅10^−4

В этом формате вещественное число (R) представляется в виде произведения мантиссы (m) и основания системы счисления (P)
в целой степени (n), называемой порядком.

Представим это в общем виде, как: R=m⋅P^n.

Порядок n указывает, на какое количество позиций и в каком направлении должна сместиться в мантиссе точка (запятая),
отделяющая дробную часть от целой. Мантисса, как правило, нормализуется, то есть представляется в виде правильной дроби
0 < m < 1.

Мантисса должна быть правильной дробью, у которой первая цифра после точки (запятой в обычной записи) отлична от нуля.
Если это требование выполнено, то число называется нормализованным.

В 4-байтовом формате представления вещественного числа первые три байта выделяются для размещения мантиссы, в четвертом
байте размещаются порядок числа, знаки числа и порядка.

<div className="flex justify-center">
    <img src="/images/tsis/4-byte-real-number-representation.png" />
</div>

# 16. Архитектура компьютера. Компьютер классической архитектуры

Архитектура вычислительной машины (Архитектура ЭВМ, англ. Computer architecture) — концептуальная структура 
вычислительной машины, определяющая проведение обработки информации и включающая методы преобразования информации в 
данные и принципы взаимодействия технических средств и программного обеспечения.

В настоящее время наибольшее распространение в ЭВМ получили 2 типа архитектуры: принстонская (неймановская) и 
гарвардская. Обе они выделяют 2 основных узла ЭВМ: центральный процессор и память компьютера. Различие заключается в 
структуре памяти: в принстонской архитектуре программы и данные хранятся в одном массиве памяти и передаются в процессор 
по одному каналу, тогда как гарвардская архитектура предусматривает отдельные хранилища и потоки передачи для команд и 
данных.

# 17. Принципы функционирования компьютера классической архитектуры

Классическая архитектура компьютера постоена на принципах Джона фон Неймана:

1. принцип программного управления (программа состоит из набора команд, которые выполняются процессором друг за другом 
в определенной последовательности);
2. принцип однородности памяти (программы и данные хранятся в одной и той же памяти);
3. принцип адресности (основная память состоит из перенумерованных ячеек и процессору в любой момент времени доступна 
любая ячейка).

# 18. Структура компьютера

**Компьютер** состоит из **процессора**, **памяти** и **интерфейса устройства ввода/вывода** (+ *периферийные устройства*).

*Устройства* в **процессоре** подключаются в **системную шину**. **Системная шина** предствляет собой *среду передачи данных*.

**Память** - это устройство в котором хранятся *команды* и *данные*, которые закодированы в *двоичном коде*. *Команды* и
*данные* ничем не отличаются друг от друга. Обрабатываются одним и тем же устройством, распространяются одним и тем же 
путем. В **памяти** расположены команды последовательно *друг за другом*.

# 19. Системная шина компьютера

Системная шина. Это основная интерфейсная система компьютера, обеспечивающая сопряжение и связь всех его устройств между собой.

Системная шина включает в себя:

1. кодовую шину данных (КШД), содержащую провода и схемы сопряжения для параллельной передачи всех разрядов числового кода (машинного слова) операнда;
2. кодовую шину адреса (КША), включающую провода и схемы сопряжения для параллельной передачи всех разрядов кода адреса ячейки основной памяти или порта ввода-вывода внешнего устройства;
3. кодовую шину инструкций (КШИ), содержащую провода и схемы сопряжения для передачи инструкций (управляющих сигналов, импульсов) во все блоки машины;
4. шину питания, имеющую провода и схемы сопряжения для подключения блоков ПК к системе энергопитания.

Системная шина обеспечивает три направления передачи информации:

1. между микропроцессором и основной памятью;
2. между микропроцессором и портами ввода-вывода внешних устройств;
3. между основной памятью и портами ввода-вывода внешних устройств (в режиме прямого доступа к памяти).

# 20. Организация системной шины

Системная шина – это шина, которая служит для физического и логического объединения всех устройств ВМ. Совокупность 
линий шины можно подразделить на три функциональные группы: шину данных, шину адреса и шину управления.

В общем, функционирование системной шины можно описать следующим образом:

1. Если одно из устройств хочет передать данные другому, оно должно получить в своё распоряжение шину и передать по ней 
данные.

2. Если какое-то устройство хочет получить данные от другого устройства, оно должно получить доступ к шине и с помощью 
соответствующих линий управления и адреса передать другому устройству запрос. Далее оно должно ожидать, пока устройство, 
получившее запрос, пошлёт данные.